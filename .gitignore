# ── Python / sidecar ──
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# ── Node / 前端 ──
node_modules/
dist/
.vite/

# ── Tauri / Rust ──
target/
apps/desktop/src-tauri/target/

# ── 模型权重缓存（体积大，不入库；首次运行自动下载）──
.cache/
models/

# ── 本地配置 / 密钥（API key 等绝不入库）──
.env
.env.local
*.key

# ── 操作系统 ──
.DS_Store
Thumbs.db

# ── IDE / 编辑器 / 工具 ──
.idea/
.vscode/
.claude/

# ── 打包产物 ──
*.dmg
*.msi
*.AppImage
*.deb
